// Integration tests go here
// Even in integration tests, all external network calls should be mocked
import { SampleComponent } from './SampleComponent'

describe('SampleComponent Integration Tests', () => {
  it('should work', () => {
    expect(SampleComponent).toBeDefined()
  })
})
